import 'package:flutter/material.dart';

class BasicGoogleMap extends StatelessWidget {
  const BasicGoogleMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}